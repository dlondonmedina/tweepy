<%= form_with(model: tweep, local: true) do |form| %>
  <% if tweep.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tweep.errors.count, "error") %> prohibited this tweep from being saved:</h2>

      <ul>
        <% tweep.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :tweep %>
    <%= form.text_area :tweep %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
